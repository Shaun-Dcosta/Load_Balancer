apiVersion: v1
kind: Service
metadata:
  name: url-shortener-svc
spec:
  selector:
    app: url-shortener 
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 5000 
      # nodePort: 30007 # Keep if using NodePort
  type: LoadBalancer # Or NodePort